// /tasks — Tasks & Reminders.
// Placeholder.

export default function TasksPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center p-8 gap-4">
      <h1 className="text-3xl font-bold">Tasks &amp; Reminders</h1>
      <p className="text-muted-foreground">Deadlines and reminders coming later in Phase 12.</p>
    </main>
  );
}